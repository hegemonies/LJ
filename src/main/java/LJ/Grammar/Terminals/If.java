package LJ.Grammar.Terminals;

public class If implements Terminal {
    private static final String value = "if";

    @Override
    public String getValue() {
        return value;
    }
}
