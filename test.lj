public class Test {
    int getMin(int[] arr, int size) {
        int i = 0;
        int min = 999999;
        i = 1;
        while (i < size) {
            if (array[i] < min) {
                min = array[i];
            }
            i = i + 1;
        }

        return min;
    }

    public static void main(String[] args) {
        int[] array = new int[3];
        array[0] = 2;
        array[1] = 0;
        array[2] = 3;
        int min = getMin(array, 3);
    }
}